export interface Profile {
	userId: string,
	nickName?: string,
	firstName?: string,
	lastName?: string,
	image?: string,
	email?: string
}

export interface ProfileData {
	nickName?: string,
	firstName?: string,
	lastName?: string,
	image?: string,
	email?: string
}