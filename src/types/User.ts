// Creation of keys must be same as other types. If uses quotes, all of your keys must be quoted.
// Indention must be the same. If uses spaces, then all of your indention must be spaces.

interface User {
	"id": string;
	"username": string;
	"createdAt": string;
	"activeAt": string;
}

export type { User }
